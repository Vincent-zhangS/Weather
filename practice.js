const calculate = (a, b) => { a + b; };
let balance = 500;
let deposit = 500;
let total = calculate(balance, deposit);
let age = 30;
let likesPizza = true;
let stringType = "bar";
let booleanType = true;
let integerType = 1;
let floatType = 1.5;
let nullType = null; //Declares something empty
let undefinedType = undefined; //Declaring unintentional absence of value
let weatherDetail = {
    weather: "sunny",
    zipcode: "00000",
    temp: 30
};
let nyWeather = {
    weather: "Clear",
    zipcode: "10001",
    temp: 45
};
const getWeatherDetail = (props) => {
};